<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.ssafy.happy.repository.mapper.AdminMapper">

	<select id="getAllViews" resultType="int" parameterType="int">
		SELECT COUNT(*) as "count" FROM views
		group by MONTH(regdate) 
		order by regdate
		limit 0, #{month}

	</select>

	<select id="getRealtime" resultType="realtime">
		SELECT dong, COUNT(*) as "count" FROM realtimesearch
		group by dong
		order by COUNT(*) DESC
		
		limit 0, 5

	</select>

</mapper>